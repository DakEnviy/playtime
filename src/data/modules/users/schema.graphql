schema {
    query: Query
}

type Query {
    me: User
    user(userId: ID!): User
}

type User {
    id: ID!
    role: UserRole!
    username: String!
    avatar: URL!
}

enum UserRole {
    Default
    Moderator
    Admin
}
